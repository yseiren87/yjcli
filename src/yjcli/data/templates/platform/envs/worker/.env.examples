# Service display name
NAME=

# Semver or build version
VERSION=

# Optional listen bind (add only when this service exposes a port)
# HOST=

# Optional listen port (add only when this service exposes a port)
# PORT=

# Local command used only by platform scripts/run.* (.env.local-dev)
RUN_COMMAND=
